function Elevadoa3(){

    
    for (let contadora = 1; contadora <= 15; contadora++) {
        let result = Math.pow(contadora,3)
     alert(contadora + ' elevado a 3 é igual a: '+ result)
    }
    document.getElementById('resposta').innerHTML = contadora + ' elevado a 3 é igual a: '+ num;
}
